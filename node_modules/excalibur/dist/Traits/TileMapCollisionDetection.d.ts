import { Trait } from '../Interfaces/Trait';
import { Actor } from '../Actor';
import { Engine } from '../Engine';
export declare class TileMapCollisionDetection implements Trait {
    update(actor: Actor, engine: Engine): void;
}
